# How to build

1. Clone the repository

    ```sh
    git clone https://github.com/odesai840/TinyRT.git
    cd TinyRT
    ```

2. Run the appropriate setup script

    Windows:

    ```sh
    cd scripts
    .\build.bat
    ```

    Linux:

    ```sh
    cd scripts
    ./build.sh
    ```
